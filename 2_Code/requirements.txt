# ============================================================
# 计算物理期末项目 — 混沌双摆数值模拟
# 环境依赖清单
# ============================================================
# Python 版本要求: >= 3.10
#
# 安装方式:
#   pip install -r requirements.txt
#
# 推荐使用虚拟环境:
#   python -m venv venv
#   source venv/bin/activate      (Linux / macOS)
#   venv\Scripts\activate         (Windows)
#   pip install -r requirements.txt
# ============================================================

numpy>=1.24.0
scipy>=1.11.0
matplotlib>=3.7.0

# 可选加速依赖 (第 3 阶段参数空间扫描时使用)
# numba>=0.58.0

# 可选: 动画导出为 MP4 需要 ffmpeg (系统级安装)
# https://ffmpeg.org/download.html
